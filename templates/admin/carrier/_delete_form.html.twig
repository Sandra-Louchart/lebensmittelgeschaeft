<form method="post" action="{{ path('app_carrier_admin_delete', {'id': carrier.id}) }}" onsubmit="return confirm('Möchten Sie dieses Element wirklich löschen?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ carrier.id) }}">
    <button class="btn">Löschen</button>
</form>

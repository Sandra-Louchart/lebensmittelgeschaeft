{% extends 'base_admin.html.twig' %}
{% block stylesheets %}
    {{ encore_entry_link_tags('admin') }}
{% endblock %}

{% block title %}Promo{% endblock %}

{% block body %}

<section class="blockAdmin">
    <h1 class="title text-center mb-5 mt-xxl-5">Promo</h1>
    <div class="container">
        <table class="table table-responsive-lg text-dark">
            <tbody>
            <tr>
                <th>Code</th>
                <td>{{ promo.code }}</td>
            </tr>
            <tr>
                <th>Angebote</th>
                <td>{{ promo.reduction }}</td>
            </tr>
        </tbody>
    </table>
        <a class="col btn btn-primary mb-sm-2 mb-2 btn" href="{{ path('app_admin_promo_index') }}">Zurück zur Liste</a>

        <a class="col btn btn-primary mb-sm-2 mb-2 btn" href="{{ path('app_admin_promo_edit', {'id': promo.id}) }}">Bestätigen</a>


    {{ include('admin/promo/_delete_form.html.twig') }}
    </div>
</section>
{% endblock %}

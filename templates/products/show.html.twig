{% extends 'base.html.twig' %}

{% block stylesheets %}
    {{ encore_entry_link_tags('app') }}
    {{ encore_entry_link_tags('product') }}

{% endblock %}

{% block title %}{{ product.name }}{% endblock %}

{% block body %}
<section class="blockAdmin">
    <h1 class="title text-center mb-5 mt-xxl-5">Products TESTTTT</h1>
    <div class="container">

        <table class="table table-responsive-lg text-dark">
            <tbody >
            <tr>
                <th>Name</th>
                <td>{{ product.name }}</td>
            </tr>
            <tr>
                <th>Picture</th>
                <td>
                    <img src="/images/products/{{ product.pictureName }}" alt="{{ product.name }}" class="idImage img-thumbnail rounded float-start">
                </td>
            </tr>
            <tr>
                <th>Subtitle</th>
                <td>{{ product.subtitle }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ product.description }}</td>
            </tr>
            <tr>
                <th>Price</th>
                <td>{{ product.price }}</td>
            </tr>
            <tr>
                <th>UpdatedAt</th>
                <td>{{ product.updatedAt ? product.updatedAt|date('d-m-Y') : '' }}</td>
            </tr>
            </tbody>
        </table>

        {{ dump('product') }}
    <section class="blockBody">
        <div class="row">
            <div class="col-md-3">
                <h3>Filter</h3>
            </div>
            <div class="col-md-9">
                <h3>Unserer Products</h3>
            </div>
        </div>
        {#    <div class="container">#}
        {#        <div class="row justify-content-center text-center">#}
        {#            <div class="col-md-8 col-lg-6">#}
        {#                <div class="header">#}
        {#                    <h2>Unserer Products</h2>#}
        {#                </div>#}
        {#            </div>#}
        {#        </div>#}
        {#        <div class="container-fluid pt-5 text-center">#}
        {#            <div class="row justify-content-around">#}
        {#                #}
        {#            </div>#}
        {#        </div>#}
        {#    </div>#}
    </section>

{% endblock %}
